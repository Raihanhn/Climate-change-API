# Climate-change-API

First create a Folder.
run npm init.
create index.js file (server).
run npm i cheerio.
run npm i express.
run npm i nodemon.
In package.json:  "scripts": {
    "dev": "nodemon index.js"
  },
